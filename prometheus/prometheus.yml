global:
  scrape_interval: 15s        # Scrape every 15 seconds (good for thesis demos)
  evaluation_interval: 1s     # rule evaluation must not dominate the RQ2.3 latency budget

rule_files:
  - "ras_alerts.rules.yml"    # RQ2 threshold-based fault detection rules

scrape_configs:
  - job_name: 'ras_drone_sim'
    static_configs:
      - targets: ['localhost:8000']
        labels:
          system: 'drone_sim'
          environment: 'thesis_experiment'

  - job_name: 'ras_drone_live'
    scrape_interval: 1s        # 1 Hz, per the Study 2 data collection protocol (RQ2)
    static_configs:
      - targets: ['localhost:8001']
        labels:
          system: 'drone_live'
          environment: 'rq2_physical_drone'
